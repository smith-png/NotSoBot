// Central color palette for NotSoBot embeds. Update the hex values here to
// re-theme every embed in the bot — nothing elsewhere hardcodes a color.

module.exports = {
  // General / success / informational embeds (help, sticker confirmations,
  // year-select embed, default embed-editor color, etc.)
  brandColor: 0xffa100, // NotSoBot orange

  // Error messages and staff-only notices (permission-denied, validation
  // failures, cooldowns, owner-only info panels).
  alertColor: 0x1e328f, // NotSoBot blue
};
